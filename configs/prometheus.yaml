global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'madara-appchain'
    static_configs:
      - targets: ['otel-collector:9464']
        labels:
          stack: 'drone_swarm'
