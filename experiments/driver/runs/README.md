# Run outputs

Each run writes `<label>-<timestamp>.jsonl` (one line per submission) and,
on clean termination, `<label>-<timestamp>.summary.json` (throughput,
mean/p50/p90/max latency, per account). Reported figures come from the
summaries.
